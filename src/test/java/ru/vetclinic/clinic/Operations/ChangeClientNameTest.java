package ru.vetclinic.clinic.Operations;

import org.junit.Test;

import static org.junit.Assert.*;

/**
 * Не закончен
 * Created by Djony on 30.07.2016.
 */
public class ChangeClientNameTest {
    /*@Test
    public void simpleChanging() throws Exception {
        Clinic clinic = new Clinic();
        Client client1 = new Client("vova");
        Client client2 = new Client("tolya");

        String s1 = "addnewclient vova";
        clinic.doOperation(s1);
        String s2= "changeclientname vova tolya";

        assertTrue(clinic.getClients().contains(client1));
        clinic.doOperation(s2);
        assertFalse(clinic.getClients().contains(client1));
        assertTrue(clinic.getClients().contains(client2));
    }*/
}